
{% extends 'layouts/base.html.twig' %}

{% block title tarifs.title %}

{% block body %}
    <section class="show">
        <div class="row mt-4">
            <div class="col-md-6 mx-auto">
                <article class="blog  rounded ">
                    <div class="mw-100 rounded-top overflow-hidden">
                        <a href="{{ path('app_tarifs_show', {id: tarifs.id}) }}">
                        </a>
                    </div>
                    <h1 class="h3 pl-3 pt-3 mt-4">
                        <a href="{{ path('app_tarifs_show', {id: tarifs.id}) }}"
                           class="text-secondary text-decoration-none">{{ tarifs.title }}
                        </a>
                    </h1>


                </article>

            </div>
            <div class="mt-3 ">
                <p class="px-5 pb-3 h4">{{ tarifs.description }}</p>
            </div>
        </div>

    </section>
{% endblock %}